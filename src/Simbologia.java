public class Simbologia {
    /*
    (\)
    ! not
    printf("Tu promedio es %.1f \n"= salto de renglon
    final double/int darle valor exacto a la variable
    == Es igual
    != Es distinto
    <, <=, >, >= Menor, menor o igual, mayor, mayor o igual
    &&= Operador and (y)
    ||= Operador or (o)
    != Operador not (no
    % o mod= Resto de una división entre enteros
    if(e.compareTo(mayor)>0){//compara
    Math.sqrt (#) =sacar raices
    Math.pow(#,#)= elevar a cualquier numero
    Math.max(#, #)= maximo entre 2 numeros
    ("variable" + "variable" ? " "texto" " : " "texto "));//if-else//se usa para sustituir esto
    System.out.println("Ingresa la letra: ");
      char letra=entrada.next().charAt(0);//asi se declaran letras
    Character.isUpperCase// ver si una letra es mayuscula
     */
}
